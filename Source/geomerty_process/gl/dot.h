#pragma once 

  /// Computes out = dot(a,b)
  /// @param[in] a  left 3d vector
  /// @param[in] b  right 3d vector
  /// @return scalar dot product
    double dot(
    const double *a, 
    const double *b);
